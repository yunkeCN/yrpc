syntax = "proto3";

package base;

import "google/api/annotations.proto";

service Ping {
  rpc Echo(EchoMessage) returns (EchoMessage) {
    option (google.api.http) = {
      get: "/echo"
    };
  }
}
message EchoMessage {
  string text = 1;
}